#' Estimated Sample Effect
#'
#' Estimates sample effect from the expressions of the randomized data
#'
#' @param r.data randomized expression dataset, rows as probes, columns as samples.
#' @return an estimation of the sample effect
#' @keywords data.setup
#' @export
#' @examples
#' smp.eff <- estimate.smp.eff(r.data = r.data.pl)


"estimate.smp.eff" <- function(r.data) return(r.data)
